user = input("What is your sentence?")

def my_function():
  print("
        ")





Create a program with a function that that takes in a users 
string and uses a shift cypher to turn the string into a secret code. 

BONUS: 

Write another function that decodes someone else's secret message. 